# i3status configuration file.
# see "man i3status" for documentation.

# It is important that this file is edited as UTF-8.
# The following line should contain a sharp s:
# ß
# If the above line is not correctly displayed, fix your editor first!

# General configs
general {
  output_format = "i3bar"
  colors = false
  interval = 5
  markup = "pango"
}

# Orders
order += "time"
order += "volume master"
order += "ethernet _first_"

# Configs
time {
  separator = false
  format = " %H:%M "
}

volume master {
  separator = false
  device = "pulse"
  format = "<span font_desc='Open sans 10' foreground='#f8f8f2'> 🎵 %volume </span>"
  format_muted = "<span font_desc='Open sans 10' foreground='#f8f8f2'>  </span>"
}

ethernet _first_ {
  separator = false
  format_up = "<span font_desc='Open sans 10' foreground='#f8f8f2'>  %ip </span>"
  format_down = " Disconnected "
}